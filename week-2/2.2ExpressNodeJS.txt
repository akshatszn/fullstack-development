// NODE.JS AND ITS RUNTIME**********
//  Ecmascript, Javascript, Node.js and bun are all different things.
// mainly shyd backend h ye

/* ECMASCRIPT is the scripting language specification on which Javascript is based.
 It is a documentation / website that tells ki how javascript wi;ll be written etc.
 These guys are the owners of the jkavascript standard.**** Basically ek rulebook likhi h for JS.
 BASICALLY THIS RULEBOOK SAYS KI IF YOU ARE MAKING A COMPILER OR SOFTWARE TO RUN JS THEN PLEAS FOLLO THESE RULES.
 AND SHOULD BE UP TO DATE TOO WITH THE ECMASCRIPT STANDARD.

 JAVASCRIPT is a scripting language conforming these rules.
 JS is the most known and wildly used implementation of ECMAscript.
 JS also includes things like DOM(Document object model) which are not specified by ECMAscript.
 The browsers give us many things on top for better code flow. (Yahi woh cheezein h jo extra h other than ECMA)
 Like for example seTimeout, fs.readfile  etc......

 Some popular JS compilers: V8 in chrome, SpiderMonkey in firefox. (Takes the JS code and converts to zeros and ones)
 // jAB BHI ECMASCRIPT STANDARD UPGRADE HOGI TOH OBV INHE APNE COMPILER IN BROWSER KO BAND KR DIJIYEGA.

 THO INTERVIEWS MEI PUCHTE H KI IF JS CAN BE COMPILED, TOH USKA ANSWER NO. BY WRITING COMPILERS ABOVE I MEAN THINGS THAT CONVERT JS CODE TO ZERO AND ONE CODE FOR MACHINE LANGUAGE. 

 Browsers include extra things for better code experience.
*/

/*
  WHAT IS HTTP *********
  so basically,
 Frontend code we write in HTML, CSS, JavaScript

 Backend code we write using NodeJS. 
 Backedn is the section where all the rquests go.
 LIke frontend mei ek button jo click krke google ke data center mei request fetch krke laayega woh backend se laara h.

 And HTTP does the job of connecting the frontend to the backend to give immediate results.  *********
 HTTP is the protocol that lets your frontend and backend talk.

*/ 

//Today's Goal is to write an HTTP server in node.js ******************
// (Once we write this code, we can run it in a datacenter. We can deploy it in a datacenter in USA)  
// We'll also learn how to communicate with it later in the class.

/* Use cases of Node.js
 Create Clis
 Create a Video Player
 Create a game
 Create an HTTP server
*/

// frontend talks to backends through WIRES & ROUTERS. Layed down all over the globe ofcourse.
// Process is click on some button "search" and then a request is sent to the main data center (Of most tech companies located in USA)
// and then a response comes back.
// All this happens without the interaction of humans, just through code. OBV.

// All the above four lines are being done using HTTP protocol.

/* HTTP protocol (HTTP servers fight HTTP protocol)
 In the end, its the client throwing some information at a server. Server doing something with that information. Server responding back with the final result.

 Think of them as functions where 
 1. Arguments are something the client sends. (Takes input from someone on the internet.)
 2. Rather than calling a function using its name, the client uses a URL.
 3. Rather than the function body, the server does something with the request.
 4. Rather than the function returning a value, the server responds with some data.
 
 Hence HTTP server is very much like a function, just that functions are 
 */

/* Now what is HTTP server. 
 The thing that exposes the machine learning model to the world.
 Some code that is able to follow the HTTP protocol and is able to communicate with clients (browsers/mobile apps)

 Think of this to be similar to the call app in your phone which lets you communicate with your friends
*/
// Maybe calling as a process is HTTP protocol and all the caller apps that let us call another person are 
// Can someone in rajasthan hit my http server that i just created?? OFcourse not.
// We need Public IPs. Its a big process basically. 
// But yea I can ask my friend who might be on the same wi-fi to hit my server up.
// Proper deploying process has a lot more to it.
// HTTP server is like a function, 

// HTTPS is just a secure version of http server.
// While sending data from one point to another, people can sniff on that data. Toh not good.

/* In HTTP server/ protocol,
  
 THings a client needs to worry about:
 1. Protocol (HTTP/ HTTPS) , the protocol which they are following. 
 2. ADDRess/ URL of the backend server, any URL is the URL of backend server hee. Abd frontend ka poora code backend server se hee aata h.
 3. Route
 4. Headers, body, Query Parameters
 5. Method
 While sending, We need to take take all the things mentioned above in consideration.

 Things the server needs to worry about:
 1. Response headers.
 2. Response body.
 3. Status codes.
 // All three are parts of responses that backend is sending obviously.

 Communications happen like:
 (FROM CLIENT'S POV)
 https//:chat.openai.com/backend-api/conversation
 https is protocol
 chat.openai.com is URL
 backend-api/conversation is route.

 1. Header- cookie -123@abce2335D@E225,  (cookie is one of many headers we can send)
    isme we send some authentication information telling ki we are logged in.
 2. Body- what is 2+2 (usually in JSON). The arguments going in, Or the request going in.
    "What is 2+2" as a text jaayega maybe body bann ke.
    the fact that 2+2 mei do number add hone ye most prolly method btayega.
 3 Method- Like what do you want to do with that data, Like post that data, Do want to get some data etc.
 
 Chatgpt on chrome kholke send something to it then :
 In inspect window, we can see all the requests going out in the network section.
 Headers bhi dikhenege that we would be sending, Authorisation header hoga usme se ek, iss header ko duusro ke haat ni lgne de skte 
 Or else theyll be able to access our chatgpt account. DK how but they can.
 Request METHOD mei GET likkha aara hoga.cuz it is a req where i want something.

 body will be in Payload section, dhuundna pdega yahan pe agr exact text dhuundne jaayega.
 Browser store our headers jab bhi we login and send them out whenever we send a request.
 
 If someone thinks ki bc header url route method body request sab saath mei kyu ni bhej dete is because 
 Ek samajhdaar aadmi ne socha hoga ki bc authorisation, body of request and methods wagerah sab alag alag rakkho toh better rhega.
 This is called specification.

*/

// asking for something on GPT or entering a URL on browswer tab is same thing 
// as dono mei hee hum backend server se ek GET request krre.
Just that data we'll be getting differs,
browser tab pe search kiya toh html data and (cuz we wanna open a website)
And Gpt mei chat mei koi text bheja toh payload mei JSOn ke form mei data aayega kuch (obv)

URLs are just like contacts in youre phone, In the end they map to an IP.
If you ever buy a URL of your own, you will need to point it to the IP of your server.

<<< Things that happen on your server after the request is recieved.
1. You get the inputs (route, body, headers)
2. You do some logic on the input, calculate the output
3. You return the output body, headers and status code.
(You means we are reffering to ourselves as backend.)

<<< Actually the process of listening to requests is more complicated but using Express in JS and and springboot in JAVA makes it easier for us to handle all that.

<<< some common methods you can send to the backend server
!. GET // clicking on left side bar to see the history of all chats i had.
2. POST // Adding a convo
3. PUT // Updating my name in openai settings
4. DELETE

// bhot lauda lassan h just know that he doesnt know it completely too.

Common status codes that backends respond with:
1. 200 - everything is ok
2. 404 - page/route not found
3. 403 - Authentication issues
4. 500 - Internal server error 

<<< Now all the questions you might have, this that aisa kyu all that. They are like this cuz they are specified like this thats it.
Tho i didnt have any questions.  Usne likka tha toh I said.

HOw do I create a HTTP server of my own?
& How do I expose it over the internet like chatgpt.com

There is a whole method to get into someone's account using authoristion cookie.
<<< Authorisation cookie inspect mei network tab ke under request section mei milti h whenever we throw a request. U need to find it.

// Best way to learn is by coding. So lets code.

MOST IMPORTANT*****************************

NodeJS is used to create HTTP servers
Express is used to create HTTP APIs

Focus on the code. Things will get clearer in the future.
